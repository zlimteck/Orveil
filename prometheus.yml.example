global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: orveil
    scheme: http
    metrics_path: /api/metrics
    # Must match METRICS_TOKEN in your .env file
    bearer_token: VOTRE_METRICS_TOKEN
    static_configs:
      - targets:
          - orveil:5050
